package o_assert;

/*
 * Tùy chọn Bật và Tắt assert
 * Khi thực thi mã, bạn có thể tắt tất cả các assert bằng cách sử dụng tùy chọn -da. Bạn cũng có thể bật hoặc tắt một package cụ thể
 * (và tất cả các gói con của nó) bằng cách chỉ định tên của nó, theo sau là ba dấu chấm sau tùy chọn -ea hoặc -da.
 *
 * Ví dụ, để bật xác nhận trong một gói có tên là MyPack, sử dụng:
 * -ea:MyPack...
 * Để tắt xác nhận trong MyPack, sử dụng:
 * -da:MyPack...
 *
 * Bạn cũng có thể chỉ định một lớp cụ thể với tùy chọn -ea hoặc -da. Ví dụ, điều này bật AssertDemo một cách riêng lẻ:
 * -ea: AssertDemo.java
* */
public class b_EnableAndDisableAssert {
}
